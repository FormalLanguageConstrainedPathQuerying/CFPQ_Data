# Tasks

- [done] Task 1: analyze existing tooling.
- [done] Task 2: Create tool to automate uploading to yandex s3 that we use. It must upload local file. Credantials must be provided by user from cli. **[USER GUIDANCE]**: "ru-central1"
- [done] Task 3: Autoamte migration from google drive (we use it for some graps and grammars and want to move all to yandex) to yandex s3. Do not download all items locally. Just handle items one-by-one: donload from gdrive, uplad to yandex (use created tool), remove local copy of downloaded item. **[USER GUIDANCE]**: "Name is an unique identifier. If names are equal, graphs are the same. It must not be stored twice. You can recheck it by comparing current urls for google drive." / "Only 59 .tar.gz" / "Part of migration. Be sure that you saved mapping between graph name and new url."
- [done] Task 4: Fix the default S3 endpoint of the upload/migration tools to `https://s3.yandexcloud.net` — the verified working S3 API endpoint for the cfpq-data bucket (the ru-central1 regional hostname fails TLS validation from this network). Discovered while running the migration.
- [done] Task 5: Fix the migration re-run handling of mapping entries recorded without a hash (skipped_existing): a re-run must not compare a downloaded SHA-256 against null and abort; it must re-verify via the public HEAD check instead. Discovered while resuming the interrupted migration.
- [done] Task 6: Store colliding-name graphs under suffixed keys, keeping both. All 10 colliding names (cactus, imagick, leela, nab, omnetpp, parest, perlbench, povray, x264, xz) are different graphs (java_points_to vs field_sensitive_alias), so the "same name = same graph" assumption does not hold: the java_points_to archive keeps `<name>.tar.gz`, the field_sensitive_alias one is stored as `<rst_stem>.tar.gz` (e.g. `cactus_field_sensitive_alias.tar.gz`). **[USER GUIDANCE]**: "Suffixed keys, keep both (Recommended)"
- [done] Task 7: Fix the migration upload key prefix and complete the migration record. The migration uploaded all 59 archives to the bucket root (`<name>.tar.gz`) instead of `4.0.0/graph/<name>.tar.gz` (the layout promised by DATASET_URL, Design Decision 2), so every docs URL 404s. Fix migrate() to upload under the `4.0.0/graph/` prefix (shared constant with DATASET_URL), move the 59 already-uploaded objects to the correct prefix (verified: objects under the prefix are publicly readable via the existing bucket policy, no ACL change needed), verify all mapping URLs anonymously, and commit the migration record (rewritten docs + utils/migration_mapping.json).
- [done] Task 8: Create new one group 'RDF' with the folloing graphs: generations travel skos univ foaf atom people biomedical pizza wine funding pathways enzyme go_hierarchy go geospecies taxonomy taxonomy_hierarchy .   Move these graphs from 'CFPQ old collection'. Update respective code and docs.
- [done] Task 9: fix docs building.
- [done] Task 10: fix the remaining docs build warnings (orphan documents, duplicate .tar.gz target names, duplicate graphs label, nb2plots/theme warnings).
- [done] Task 11: Propose splitting of 'CFPQ old collection' into classes. Use grammars related to the graph as a key for classification.
- [done] Task 12: Add instructions to run tests in appropriate environment.
- [done] Task 13: Design the tool to convert old format to new one. Tool mus process graphs one-by-one without downloading full dataset. Graph need to convert are all graphs in sections RDF, C alias analysis, some graphs from Java points-to graphs. **[USER GUIDANCE]**: "Compact _i templates. But be careful. Your example of relable load_k -> load_i_k looks incorrect. load_k is already has index k, so should not be relabel." / "New version prefix" / "RDF graphs have three canonical grammars (different variations over subcalssof and type) listed on respective page. Add all of them." / "1. In call_i and return_i 'i' is an index, not part of name. 2. What is the problem with alloc_r, assign_r, load_r, store_r lables? First two is just termianls. second to must be indexed. _r is just a part of name means that edge must be reversed. So, such edges do not store derectly, because respective edges without -r enough to restore reversed edges. 3. Add broaderTransitive to all RDF where required labels represented in graph. 4. Log follow-up tasks too. Do not forget to do them later."
- [done] Task 14: Package support for new-format (mtx-per-label) graphs: download() + mtx-per-label readwrite + .cnf template read/write with indexed symbols and the _r reversed-edge convention. **[USER GUIDANCE]**: "Materialized CFG (Recommended)" / "What? how label may have form load_i_5? load is an indexed lable. So _i is a placeholder for real index." / "the same for call_i, ret_i, store_i" / "Well. If you are about file name, then ok. But it is just file name, not edge label exactly." / "file name may be load_i_5.mtx. But label is load_5"
- [done] Task 15: Bump VERSION/DATASET_URL to the 5.0.0 prefix and re-point docs download links once the conversion (task 13) is complete. **[USER GUIDANCE]**: "Keep them at 4.0.0"
- [done] Task 16: Fix stale docs stats (e.g. generations listed as 546 edges vs 273 stored) and document the label conventions (indexed symbols, _r reversed edges) in the format docs. **[USER GUIDANCE]**: "Fold into task 16 (Recommended)" — the docs/tutorial.rst fix for the new download() contract (graph_from_csv(download("bzip")) -> graph_from_mtx_dir(bzip_path / "graph")) is part of task 16.
- [done] Task 17: Fix the Sphinx ERROR in docs/utils.rst (unexpected indentation), prevent such errors from reaching dev/master, and update the add-graph skill and the graph PR/issue templates to the new mtx-per-label format. **[USER GUIDANCE]**: "Gate + CI job (Recommended)" — prevention = docs build in the pre-merge quality gate AND a CI job on push/PR.
- [done] Task 18: Group the graphs in the docs left navigation bar the same way they are grouped on the Graphs page (8 captioned toctrees instead of one flat data/* glob).
- [done] Task 19: Unify indexed-label file names to the bare <base>_<k> style (drop the _i placeholder): repack the affected legacy archives and serve them from the 5.0.0 prefix.
- [done] Task 20: Serve the whole dataset from the 5.0.0 prefix: copy the unaffected legacy archives to 5.0.0/graph/, drop the dual-prefix logic, and re-point the docs download links.
- [done] Task 21: Drop the _i placeholder file-name fallback from the package (the served archives are unified) and update the format docs.
- [done] Task 22: Add an about-category paragraph to each graph category, a citation section (repo @misc + Lodygin paper template) on the About page, and installation/overview/citation sections to the README.
- [done] Task 23: Normalize the per-graph Info tables to a single schema (Full Name, Version, Direct download, Source where known): add an audit tool and standardize all 113 pages.
- [done] Task 24: Prepare an automated, documented release process for cutting v5.0.0 on demand: a Keep a Changelog changelog, a config.py/pyproject.toml version-sync guard (pre-commit + CI), a tag-triggered PyPI publish workflow (Trusted Publishing OIDC with token fallback), release docs + skill, and an atomic bump helper that also promotes the changelog.
- [done] Task 25: Add a separate license for the dataset: the code stays Apache-2.0 and the dataset (graphs/grammars/benchmarks) is CC-BY 4.0, declared in a new LICENSE-DATA.txt, the README/docs license pages, and the package metadata.
- [done] Task 26: Build the temporal CFPQ reference-answer tool (gitignored `temporal_cfpq/`, outside the package): for every graph x in-archive grammar compute the number of reachable vertex pairs using the FastMatrixCFPQ Docker image (Zenodo 18406624) as the oracle, with one-by-one download discipline, a resumable sweep, and a values-file-only output (`results/reference_answers.csv`: 155 pairs — 144 ok, 8 oom, 3 timeout; the 15 locally-failed pairs were retried on a remote 24-core/124 GB PC under `/mnt/jblab2-disk/gsv_cfpq`, recovering 4 more). User decisions: Docker installed by the user (no-sudo via the docker group), output = results CSV only, container README fetched at implementation time.
- [done] Task 27: Add `materialize_grammar()` convenience function (load .cnf template + materialize over graph in one call) for CFPQ algorithms that do not support indexed grammars; fix `_r_i` reversed-indexed terminal handling in `materialize` (e.g. `load_r_i` -> `load_k_r`); add tests for all three indexed grammar families (fsjpt, cscvf, fsca). Verified that all 5.0.0 archive .cnf files already contain the compact indexed templates (no re-upload needed) and that existing reference answers are correct (solver == brute-force on gson/xz/nab_field_sensitive_alias).
- [done] Task 28: Add number of reachable pairs to the per-category graph tables on the website; mark unavailable data explicitly ("not available", no OOM/timeout details); distinguish empty cells (grammar not applicable) from "not available" cells (grammar applicable, data missing); add one column per relevant grammar per category; provide the overall summary table (one big table for all graphs × grammars) as a downloadable resource from both the package API and the website.
- [done] Task 29: Analyze site navigation system. It looks non-homogenious. We have top, left-side and rigt-side navigation bars. Propose ways to more homogenious navigation. Navigation must be clear.
- [done] Task 30: I see that you have some troubles with git workflow. Current dev state is ok. Looks you use rebase, isnt it? Can we update skills and instructions to fit it with actual flow.
- [done] Task 31: Check correctness of cross-references and links on the site. At least we have the following issue: https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/122 Is there exist a way to systematicaly avoid cross-reference and links problem lyke one from the issue? **[USER GUIDANCE]**: "Well. Go. But by default linkcheck WITHOUT ignore. We can wayt, we want to preserve correcness."
- [done] Task 32: Cool. Yes, fix Babel warnings. Analyze rest warnings. E.g. why you suppress aernings refs.citations and ref.footnote? Can we fix it and other warnings? I want to set warings as errors and go to 'no warnings policy'. Without suppresuins if possible. **[USER GUIDANCE]**: "Well. Try to find solution that does not require workarounds or checkers modification. If site o code refactoring required for this way, propose it to me. It may be easier to do smoll refactoring, rather than add workaround. If refactoring improve and simplify site and code, it is better that wotkaround or tooling patching." / "9 approved. Go."
- [done] Task 33: Check developer documantation. Check that all parts of issue https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/76 well-documented, necessary parts added to README. **[USER GUIDANCE]**: "Let use docs as the source of truth, skills as thin pointers. Add this to main pronciples for future skills design and update."
- [done] Task 34: Prepare v5.0.0 release (1/2): analyze the code with respect to the issues https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/122 https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/76 https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/75 https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/74 https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/28 https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/26 https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/16 https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/2 and add respective references to the Changelog for the resolved ones. **[USER GUIDANCE]**: "Reference partial, no close claim" (issue #2: only the Zheng & Rugina C alias analysis data is in; the Vedurada 'Batch Alias Analysis' data is missing) / "Reference resolved sub-items only" (umbrella issue #28: cite the resolved sub-issues #27, #32, #20, #30, #33, #26 individually; do not claim #28 done).
- [done] Task 35: Prepare v5.0.0 release (2/2): cut the v5.0.0 release per the release skill (bump_version, PR dev->master, tag on origin/master, push, verify PyPI + GitHub Release). **[USER GUIDANCE]**: "Yes, both tasks. But do not forget to ask me to assist with access to PyPI. Guide me step-by-step. And do not forget that I must review and merge requext to master manually, so, tag + push and PyPI/GitHub release only after I confirm that requies merged. Moreover you should pull master irst to create tag on respective commit." / "Trusted publisher added. Go." / "Update all used actions (in GH workflows) to lates versions. Just add commit to this request. Ather that request my assistance again." / "CI report set of warnings of forn '<unknown>:1: WARNING: py:class reference target not found: networkx.classes.multidigraph.MultiDiGraph [ref.class]' Fix it" / "Well. Can we automatically trigger publishing to testPypi on pr to main to avoid such problems? So, you can check publishing to testPyPi automatically and after that I assist to merge PR." / "I can add trusted publisher for testPypi too. Is it a good way?" / "Is it safe to merge this pr? It will trigger new release that is not requiret (I guess)."
- [done] Task 36: Verify that all you experince and knolage on releases are documented. Next release must be fast and smooth. **[USER GUIDANCE]**: "Be carefull. Split global knolage useful for all developers (it must be in docks) and local knolage for you (it must be in skills)"
- [done] Task 37: Migrate project to UV manager (https://github.com/astral-sh/uv) WE need full migration: no requirements.txt, no other legacy sfuff. Configure project to use UV-provided tools for quality checking: linter, type checker, etc. Do not forget to update documentation. **[USER GUIDANCE]**: "Read https://docs.astral.sh/uv/guides/integration/pre-commit/ can we use official UV pre-commit hooks?" / "Note on ty typechecker. Let use ty for fast check but in CI use both ty and pyright." / "Delete both (Recommended)" (broken legacy scripts utils/fetch_dataset.py and utils/update_dataset_tables.py)
- [done] Task 38: We use mechanism to close issues automatically described here: https://github.blog/news-insights/product-news/closing-issues-via-commit-messages/    Improve imstructions to write respective commut message when task is link to issue.
- [done] Task 39: Fix https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/94
- [done] Task 40: For the site, add archive sizes (size to download) to tables with graphs. Add one new column to each table (for each category). Choose one unit of neasure for all graphs (Kb, Mb or Gb). Moreover, improve tool that uploads new graph. It should compute archive size. When new raph added, table must be updated. So computed size used for it. **[USER GUIDANCE]**: "Include old graphs (Recommended)" / "Add a reusable utils script"
- [done] Task 41: Analyze structure of the site. Looks like better place for canonical grammars description is category page, not graph. Because grammar canonical for category. **[USER GUIDANCE]**: "On issue and request teplamtes. If graph for existing category, then ok. But we can have at least two more options. Graph for new category. Then grammars should be provided. New grammar for existing category." / "Nothing (Recommended)" (graph pages keep no grammar section or pointer) / "Leave as-is (Recommended)" (docs/old_graphs legacy pages untouched) / "All 4 grammars (Recommended)" (rdf category page lists the union incl. broaderTransitive) / "Yes, add it (Recommended)" (Category row in both graph templates)
- [done] Task 43: One big flat table for reachable pairs on the site looks ugly. One table may be usefulf to donload for automatic processing, but it may be better to slit rendering on the sute to several tables. Eg. per category. **[USER GUIDANCE]**: "Go. Do task 43" (scope per docs/flpq.rst and tasks/global_plan.md: per-category tables on the reachable-pairs page, the flat CSV stays the single source of truth for automatic processing and gains a category column, a utils/ generator keeps the per-category tables and the category-page count columns in sync with the CSV)
- [done] Task 42: Currently the project covers only context-free queryes (CFPQ). We need to extend it to cober also regular queries (RPQ) and queries with multiple context-free grammrs. Design structure of the site and the project. I propose to reuse existing structire designrd for CFPQ for all classes of queries. So we need to add one level of hierarchy. LEt name general queries as FLPQ: Formal language constarined path queying. **[USER GUIDANCE]**: "Rename to flpq_data (Recommended)" (new PyPI distribution flpq-data at 6.0.0; cfpq-data becomes a thin deprecation shim) / "Shared catalog (Recommended)" (one Graphs section, single source of truth; per-class sections cross-link applicable graphs) / "MCFG or MCFL is not a several CFG-s. It is a class of languages that are wider than CFG. Look at papers by Hiroyuki Seki. So, we use these languages to specify constraints exaclty like other languages (regular of context-free). Obe of applications of MCFG constraomts is static code analysis \"Program Analysis via Multiple Context Free Language Reachability\" (https://dl.acm.org/doi/10.1145/3704854)." / "Design doc only (Recommended)" (this task produces the approved FLPQ design in docs/ plus a follow-up task list; implementation happens in separate tasks) / "1. One big flat table for reachable pairs on the site looks ugly. One table may be usefulf to donload for automatic processing, but it may be better to slit rendering on the sute to several tables. Eg. per category. 2. Looks your knolage about MCFG is weak. Investigate it deeply. Especially gramar format. It is important question and I think that we should use datalog-like syntax to specify MCFG-s." / "Let do tasks that does not require global reworking of project first. Eg we can refactor reachable pairs table representation on the site. Do not plan 6.0 release as a task. We must do much more tasks first. Check can we use existing librarise to parse datalog-like synatx of MCFG." / "lark (Recommended)" (parser for the .mcfg format) / "Go. When task 42 will ready, provide link to design document to discuss proposed design desisions with team members."
- [done] Task 44: MCFG readwrite module with lark: `cfpq_data/grammars/readwrite/mcfg.py` (data model, EBNF grammar, validation per the spec in `docs/flpq.rst`, `mcfg_from_text/to_text/from_txt/to_txt`), doctests with the paper's examples, reference docs page. **[USER GUIDANCE]**: "Go to task 44. Do it."
- [done] Task 45: set strong code coverage checking both in CI and locally. Set both instructionand branch coverage up to 95%. Moreover, gh is installed. remove workarounds from documentation and instructions. Extend tooling skill: no workaround for regular tasks. If there is a tool for regular task it must be installed and configured appropriately.
- [done] Task 46: RPQ query templates + seed data: first-class RPQ content — regex template entries in the existing Grammars section (Class = Regular) plus example archives, until the site restructure of task 50 moves them to the RPQ section. **[USER GUIDANCE]**: "We have no data for RPQ for now. So no real files for RPQ. So make it just documented design desision." / "For RPQ we need just design desision. Real-world data will be provided later." / "Again. No rela-world data for now. Now stubs. Just design."
- [done] Task 47: Add a `query_class` column to `reachable_pairs.csv` and the `reachable_pairs()` API (+ update the generator from task 43).
- [done] Task 51: First, analyze whether it is possible to replace some parts of developer docs and skills with refs to CI workwlow description. I think, CI must be source of thruth for some commands and knolage.
- [done] Task 52: Second, let carefully design archive structure and tool to check this structure. All archives must have sidentical structure and be self-contained. Each archive: graph as a set of MTX, description document (let design madatory cestions), all grammars (qeryes) in separated files with descriptions (in common document). Structure validation step must be added to upload workflow. Rework FLPQ design document with respect to this. **[USER GUIDANCE]**: "Do not forget to reflrct structure of archive in documnet and templates for PR and issue." "Graphs only, and plan to rework benchmarks" (the identical-structure rule applies to graph archives; benchmark data is ignored for now and gets its own rework task) / "Drop them (Recommended)" (example/template queries not tied to a graph are dropped; every query file lives inside a graph archive) / "In the upload tool (Recommended)" (upload_to_s3.py validates any graph archive before uploading, plus an audit mode over the bucket)
- [ ] Task 53: Rework benchmark page and respective data. **[USER GUIDANCE]**: "Plan task to rework benchmark page and respective data. Currently ignore benchmark data."
- [done] Task 54: Extend archive structure. Design way to specify one language in several ways: one query may be specified using different grammars. Or for CFPQ we can use grammar and RSM. RSM may be described using EBNF grammar or manually as a transition system (labelled graph with start and final staes). Investigate recursive stste machines (RSM-s) first. For each query results file must be provided: boolean mtx with constrained reachability facts (if there is a path from i to j that satisfie respective quey, this fact is in mtx). One file per quey, not for grammar or rsm: all ways to specify query must leads to identical results. Just desidn documents and tooling, no re-upload. **[USER GUIDANCE]**: "cfpq/ and rpq/" (an .rsm specification file may live in both class directories; in rpq/ the RSM must be regular — no transition label may be a nonterminal) / "What about queries/<class>/<query>/results.mtx Each query can has several representations (gramars, rsm-s) but result is independent from representation. It depends on language (query). <query> ia s common directory for langauge that contains all representations."
- [done] Task 55: Improve new data providing mechanisms. I think, main way is a link to archive on google drive. This archive must be prepared acoording to check archive structure tool. Some components may be missed (eg, if user provides new quey for existing graph, he must provide only query representation an references (number of pairs, results mtx), descriptions for readme). Structure must be preserved to simplify merge new data to existing archive. Modify issue abd request teplates to make this way the main one. Add references to respective tooling to simplify arcive preparation for new users. Extend tooling if necessary. **[USER GUIDANCE]**: "queries/ + README fragment (Recommended)" (a partial archive contains <graph>/queries/<class>/<query>/{representations, results.mtx} plus a queries/README.md holding only the new '## <class>/<query>' sections) / "Yes, build it (Recommended)" (utils/merge_archive.py: merge a partial archive into an existing one — collision checks, README section append, full re-validation of the result)
- [done] Task 56: Analyze https://arxiv.org/abs/2401.11029 For two classes it contains optimized versions of grammars. Extract them to add to respective archives. To check use temporal_cfpq. It must returns equlas result for original and optimized version. Check it on several small graphs. **[USER GUIDANCE]**: "Keep old structure (Recommended)" (re-upload the 41 legacy archives as-is with grammar/<x>_muravev2024.cnf added; upload via the upload_file() API — the CLI structure gate stays for new data; migration to the queries/ layout remains task 48) / "Cite the paper in the name" (java_points_to_muravev2024.cnf, c_alias_muravev2024.cnf)
- [ ] Task 57: Fix https://github.com/FormalLanguageConstrainedPathQuerying/CFPQ_Data/issues/127 and add tests to prevent similar problems. **[USER GUIDANCE]**: "Literal params + plain subscripts (Recommended)" (parameter names -> literal style per the PR template; reverse labels in math blocks -> plain a_r / d_r subscripts) / "Targeted rule + evaluate latex2mathml (Recommended)" (deterministic zero-dep guard now; trial latex2mathml on the corpus and adopt only if zero false positives)
